project_id = "syncon"

[auth]
# Chrome creates a temporary identity before Android approves the QR request.
enable_anonymous_sign_ins = true

[auth.rate_limit]
# Pairing needs at most one anonymous identity per installation. Keep the
# project-level allowance deliberately below Supabase's default of 30/hour/IP.
anonymous_users = 10
